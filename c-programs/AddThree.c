#include<stdio.h>

int main(){
  int a, b, c, sum;
  scanf("%d %d %d",&a,&b,&c);
  sum=a+b+c;
  printf("%d", sum);
 }
//Use VALUE when the function only needs to read/use the data.
// Use ADDRESS when the function needs to change/store data inside your variable.